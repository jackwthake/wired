# The Wired

No matter where you are, we are all connected.

![proto](./ss/proto-3.gif)

```bash
git clone --recurse-submodules https://github.com/jackwthake/wired.git
cd wired

./build         # to build
./build --run   # to build and run
./build --clean # perform clean build
```