#include "navi/navi.h"
#include "gfx.h"

#include <stdio.h>

void notes_init(struct window *win, struct vfs_node_t *path) {

}


void notes_update(struct window *win) {
  app_clear(win, RGB(200, 200, 200));
}


void notes_close(struct window *win) {
  
}
